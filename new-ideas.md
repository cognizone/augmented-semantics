# New Ideas / TODO

- TODO: Update Go-to-URI detection to treat `skos:OrderedCollection` as a collection.
- TODO: Update selection flows (tree, history, and cross-scheme) to recognize `skos:OrderedCollection`.
- TODO: Update orphan detection queries to include `skos:OrderedCollection` + `skos:memberList`.
- TODO: Update membership queries (concept → collections, collection discovery) to include `skos:OrderedCollection`.
