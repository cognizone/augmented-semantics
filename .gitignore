# IDE
.idea/

# Claude Code
.claude/*
!.claude/hooks/
!.claude/skills/
!.claude/settings.json

# Dependencies
node_modules/

# Stray npm lockfiles — this is a pnpm workspace (use pnpm-lock.yaml)
package-lock.json
**/package-lock.json

# Build output
dist/
*.tsbuildinfo

# Compiled JS (TypeScript outputs)
src/**/*.js

# Sandbox (local experimentation)
sandbox/

# Video-skill tooling + artifacts (hyperframes skills, /brag output) — installed
# locally by the skills system, not part of this project. Keep out of git.
.agents/
agent/
brag-output/
skills-lock.json
